global:
  scrape_interval: "15s"
scrape_configs:
  -
    job_name: "neural-operator-training"
    static_configs:
      -
        targets:
          - "neural-operator-service:8080"
    metrics_path: "/metrics"
    scrape_interval: "10s"
